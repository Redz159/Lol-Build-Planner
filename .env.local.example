# Copy this file to .env.local (already git-ignored) and fill in your own key from
# https://developer.riotgames.com/ to use the local-only "Import from Riot" feature.
# Personal keys expire every 24h and are rate-limited; this feature only works when
# running `npm run dev` — it's stripped from the production build entirely.
RIOT_API_KEY=
